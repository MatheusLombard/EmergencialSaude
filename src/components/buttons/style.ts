import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    buttonPressEntrar: {
        backgroundColor: "#fff",
        borderRadius: 18,
        width: 150,
        height: 60, 
        justifyContent: "center",
        alignItems: "center",
        marginBottom: 50, 
    
    },
    buttonEntrar: {
        color: "#000",
        fontSize: 25,
    },
})

