import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textDropDown: {
        marginLeft: 10,
        marginBottom: 3,
    },
    areaDropDown:{
        marginBottom: 10,
        borderRadius: 15,
        height: 55,
        alignItems: "stretch",
    },
});